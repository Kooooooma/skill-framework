{
  "display_name": "__SKILL_TITLE__",
  "short_description": "__SKILL_SHORT_DESCRIPTION__",
  "default_prompt": "Use $__SKILL_NAME__ with its workflow program model: initialize or resume a runtime run, use skill.machine.json for legal lifecycle transitions, use node.graph.json for topological node planning, load only the runtime context slice for the active state/node/step, run each node as a sequential workflow with numbered steps, validate outputs against contracts, and report blockers rather than skipping gates.",
  "allow_implicit_invocation": true
}
